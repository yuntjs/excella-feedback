<div class="container">
  <%= back_button(survey_templates_path) %>

  <div class="row">
    <div class="col-md-6 col-md-offset-3 col-sm-8 col-sm-offset-2">
      <h1>New Question for <%= @survey.title %></h1>
      <%= render 'form', survey: @survey, question: @question, submit_path: presentation_survey_questions_path(@presentation, @survey) %>
    </div>
  </div>
</div>
