<div class="container">
  <%= back_button(survey_templates_path) %>

  <div class="row">
    <div class="col-md-6 col-md-offset-3 col-sm-8 col-sm-offset-2">
      <h1>New Question for <%= @survey_template.title %></h1>
      <%= render partial: 'shared/question_form', locals: { question: @question_template, submit_path: survey_template_question_templates_path(@survey_template) } %>
    </div>
  </div>
</div>
