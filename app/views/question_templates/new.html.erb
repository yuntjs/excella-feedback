<div class="container">
  <%= back_button(survey_templates_path) %>

  <div class="row">
    <div class="col-md-6 col-md-offset-3 col-sm-8 col-sm-offset-2">
      <h1>New Question for <%= @survey_template.title %></h1>
      <%= render 'form', survey_template: @survey_template, question_template: @question_template %>
    </div>
  </div>
</div>
