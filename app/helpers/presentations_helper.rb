module PresentationsHelper
    # Participations
    # def add_participation user
    #   # @participation = Participation.new
    #   @presentation = Presentation.find params[:id]
    #   # debugger
    #   if @presentation.participations.where(user: user).length == 0
    #     @presentation.participations.create!(user: user, is_presenter: false)
    #   end
    # end
end
